{
  "name": "amn-pickle",
  "version": "1.0.0",
  "description": "AMN Pickle - Full Stack Payment Gateway with Razorpay",
  "scripts": {
    "install:all": "cd backend && npm install --include=dev && cd ../frontend && npm install --include=dev",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build": "npm run install:all && npm run build:frontend && npm run build:backend",
    "start": "cd backend && npm start"
  }
}
